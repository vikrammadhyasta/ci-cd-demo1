pipeline {
    agent any

    parameters {
        string(name: 'ENVIRONMENT', defaultValue: 'dev', description: 'Enter the environment (dev or prod)')
    }

    stages {

        stage('Setup') {
            steps {
                echo "Running for ENVIRONMENT: ${params.ENVIRONMENT}"
            }
        }

        stage('Conditional Stage') {
            steps {
                script {
                    if (params.ENVIRONMENT == 'prod') {
                        echo "🔥 Deploying to Production"
                        // add prod-specific logic
                    } else {
                        echo "🧪 Deploying to Development"
                        // add dev-specific logic
                    }
                }
            }
        }

        stage('Parallel Tasks') {
            parallel {
                stage('Build') {
                    steps {
                        echo "🔨 Building in ${params.ENVIRONMENT}"
                        bat 'echo Compiling code...' // simulate build
                    }
                }

                stage('Test') {
                    steps {
                        echo "🧪 Running tests in ${params.ENVIRONMENT}"
                        bat 'echo Running tests...' // simulate test
                    }
                }
            }
        }
    }

    post {
        success {
            echo '✅ Pipeline succeeded!'
        }
        failure {
            echo '❌ Pipeline failed!'
        }
    }
}
